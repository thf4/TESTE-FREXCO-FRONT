import { makeStyles } from "@material-ui/core/styles";
export const btnColor = { color: "green" };
export const styleaBar = { backgroundColor: "white" };
export const useStyles = makeStyles((theme) => ({
  root: {
    flexGrow: 1,
  },
  title: {
    flexGrow: 1,
  },
}));
export const color = {
  color:"black"
}
